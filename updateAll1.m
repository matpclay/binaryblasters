function [] = updateAll(timer,timerInfo,handles)

%global handles;

disp(handles);

% axes(handles.axes1);
% imageArray = {handles.spaceship, handles.spaceshipPosition(1), handles.spaceshipPosition(2), 1, 1};
% plotImage(imageArray);

end